#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	dh_auto_build -- make build

override_dh_auto_install:
	dh_auto_install -- make install DESTDIR=$(CURDIR)/debian/dns-tor-proxy

override_dh_systemd_enable:
	dh_systemd_enable --name dns-tor-proxy
